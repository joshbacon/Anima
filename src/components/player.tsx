import { useState } from 'react';

// make this a child of the draggable class
function Player() {

    return <div className='w-full h-full min-w-fit min-h-fit col-span-3 bg-slate-500 rounded-2xl'>
        <h2>Player</h2>
    </div>
}

export default Player;